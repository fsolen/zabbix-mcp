# ⚠️  Production'da SealedSecrets veya ExternalSecrets kullanın
# Bu dosya sadece template amaçlıdır
apiVersion: v1
kind: Secret
metadata:
  name: zabbix-mcp-secret
  namespace: zabbix-mcp
  labels:
    app.kubernetes.io/name: zabbix-mcp
    app.kubernetes.io/component: secret
type: Opaque
stringData:
  ZABBIX_USER: "api_user"
  ZABBIX_PASS: "changeme"
---
# ExternalSecrets örneği (önerilen)
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: zabbix-mcp-secret
#   namespace: zabbix-mcp
# spec:
#   refreshInterval: 1h
#   secretStoreRef:
#     name: vault-backend
#     kind: ClusterSecretStore
#   target:
#     name: zabbix-mcp-secret
#   data:
#   - secretKey: ZABBIX_USER
#     remoteRef:
#       key: secret/zabbix-mcp
#       property: username
#   - secretKey: ZABBIX_PASS
#     remoteRef:
#       key: secret/zabbix-mcp
#       property: password
